# Cybersecurity Chrome Extension Server

This project is a Node.js server for a Chrome extension that analyzes website links using ML models and MySQL.

## Setup
- See `.env` for environment variables
- See `src/` for source code
- See `tests/` for tests
